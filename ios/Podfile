platform :ios, '12.0'  # Increase this version to at least 12.0 or the required version

# Disable use of frameworks for Flutter
use_frameworks! :linkage => :static

# Flutter-specific configurations
flutter_application_path = '../' # Adjust the path if needed

# Your app's CocoaPods dependencies
target 'Runner' do
  use_frameworks! :linkage => :static
  pod 'Firebase/Core'
  pod 'Firebase/Auth'
  pod 'Firebase/Firestore'
  # Add any other necessary pods here

  # Flutter Pod dependencies (Manually managed)
  pod 'Flutter', :path => File.join(flutter_application_path, 'packages', 'flutter', 'flutter_module.podspec')
end
